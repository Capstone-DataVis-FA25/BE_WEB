// Application constants
export const AppConstants = {
    // Payload size limits in bytes
    DATASET_PAYLOAD_MAX_SIZE: 50 * 1024 * 1024, // 50MB - matching Express limit
    CHART_PAYLOAD_MAX_SIZE: 50 * 1024 * 1024, // 50MB - matching Express limit

    // You can add other application constants here as needed
    // DEFAULT_PAGE_SIZE: 10,
    // MAX_UPLOAD_FILE_SIZE: 10 * 1024 * 1024, // 10MB
};